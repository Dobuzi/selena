export const QUESTION_COUNT = 10;
export const TIME_LIMIT_MS = 20_000;
export const REVEAL_MS = 3_000;
export const MAX_PLAYERS = 4;
export const INITIAL_HP = 3;
export const BASE_CORRECT_POINTS = 100;
export const TIME_BONUS_PER_SEC = 2;
/** Drop players with no heartbeat for this long (ghost slots). */
export const PLAYER_STALE_MS = 45_000;
/** AI generation soft timeout → fallback. */
export const AI_GENERATE_TIMEOUT_MS = 8_000;
